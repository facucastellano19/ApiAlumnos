# Variables de entorno para Docker Compose y la aplicación
# Copia este archivo a .env y rellena los valores.
# ¡NO subas el archivo .env a tu repositorio de Git!

# --- Configuración de la Base de Datos ---
# Contraseña ROOT de MySQL (para la creación inicial del contenedor)
DB_ROOT_PASSWORD=your_strong_root_password

# Nombre de la base de datos que usará la API
DB_DATABASE=gestion_alumnos_api

# Usuario de la API para conectarse a la base de datos
DB_USER=api_user

# Contraseña del usuario de la API
DB_PASSWORD=your_strong_api_password

# --- Configuración de la API ---
# Puerto en el que correrá la API dentro y fuera del contenedor
PORT=3001

# Secreto para firmar los JSON Web Tokens (JWT)
JWT_SECRET=your_super_secret_jwt_key